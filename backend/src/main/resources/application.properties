# Kasalig AI - Spring Boot Backend Configuration
server.port=8080

# ===================================================================
# Supabase PostgreSQL Database
# ===================================================================
spring.datasource.url=postgresql://postgres:kasaligAI123@db.khnhnhmrwrmahnjbzqpu.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=kasaligAI123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate (PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Disable local SQL init (tables already exist in Supabase)
spring.sql.init.mode=never

